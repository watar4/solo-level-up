# 方式A（ローカル事前生成）でのみ使用します。
# 実際のキーは絶対にコミットしないでください（.env は .gitignore 済み）。
#
#   cp .env.example .env
#   # .env を開いて実値を設定
#   npm run generate -- --category "データサイエンス力" --count 5
#
# アプリ本体（GitHub Pages 公開物）はこのキーを一切必要としません。
ANTHROPIC_API_KEY=
